# Semantic Search MVP

This project implements a semantic search engine using vector embeddings.

Stack
- SentenceTransformers
- Chroma Vector Database

Pipeline
Documents → Chunking → Embeddings → Vector DB → Query Embedding → Similarity Search

Example query:
refund eligibility
